{
  "name": "hidden-garden",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --port 3001",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && firebase deploy"
  },
  "devDependencies": {
    "dotenv": "^16.5.0",
    "vite": "^4.5.0"
  },
  "dependencies": {
    "firebase": "^10.8.0",
    "firebase-admin": "^13.4.0",
    "firebase-tools": "^14.2.0"
  }
}
